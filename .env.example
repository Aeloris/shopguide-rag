# 使用方法：把本文件复制为 .env，再填入真实 key/连接串（.env 已被 .gitignore 忽略，不会上传）
# 阶段 A（离线）无需任何 key；阶段 B（真服务，docker compose 起来后）按需填：
#   - ANTHROPIC_API_KEY  切真 Claude（llm/vision.provider=anthropic）
#   - DATABASE_URL       切 Postgres 会话存储（store.provider=postgres）
#   - REDIS_URL          切 Redis 缓存（cache.provider=redis）
#   - DASHSCOPE_API_KEY  切真 embedding（embedding.provider=dashscope，需阿里云百炼 key）

ANTHROPIC_API_KEY=sk-ant-your-key-here
# DATABASE_URL=postgresql://shopguide:shopguide_dev@localhost:5432/shopguide
# REDIS_URL=redis://localhost:6379/0
# DASHSCOPE_API_KEY=sk-your-dashscope-key-here
